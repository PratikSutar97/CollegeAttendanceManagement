<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.attendancemanagementsystem">

    <uses-permission android:name="android.permission.SEND_SMS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Attendance.MarkAttendance"></activity>
        <activity android:name=".Attendance.SelectCourse" />
        <activity android:name=".ListStudentsByCourse" />
        <activity android:name=".ChangePassword" />
        <activity android:name=".FacultyHome" />
        <activity android:name=".ViewFaculty" />
        <activity android:name=".AddFaculty" />
        <activity android:name=".ViewStudent" />
        <activity android:name=".AddStudent" />
        <activity android:name=".Menu" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>